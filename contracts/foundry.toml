[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200

[profile.default.fuzz]
runs = 256

[rpc_endpoints]
zama_testnet = "${ZAMA_TESTNET_RPC_URL}"

[etherscan]
zama_testnet = { key = "${ETHERSCAN_API_KEY}" }
